enum NetworkRequestMethodType { post, get,upload }

enum RequestCompletionStatusEnums {
  success,
  noContent,
  badRequest,
  forbidden,
  unAuthorised,
  notFound,
  internalServerError,
  connectTimeOut,
  cancel,
  receiveTimeOut,
  sendTimeOut,
  noInternetConnection,
  unknownStatus,
  connectionError,
  requestResponseInValid,
  badCertificate,
  badResponse
}
