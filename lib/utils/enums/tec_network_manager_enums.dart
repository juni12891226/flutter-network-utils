enum TeCNetworkRequestMethodType { post, get, upload }

enum TeCRequestCompletionStatusEnums {
  success,
  noContent,
  badRequest,
  forbidden,
  unAuthorised,
  notFound,
  internalServerError,
  connectTimeOut,
  cancel,
  receiveTimeOut,
  sendTimeOut,
  noInternetConnection,
  unknownStatus,
  connectionError,
  requestResponseInValid,
  badCertificate,
  badResponse,
}
